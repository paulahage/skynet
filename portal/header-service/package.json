{
  "name": "header-service",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build-ui": "cd ../header-ui && npm run build && cd ../header-service",
    "copy-ui": "cp -r ../header-ui/build ./header-dist",
    "build": "npm run build-ui && npm run copy-ui"
  }
}
