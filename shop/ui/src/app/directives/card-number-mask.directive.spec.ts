import { CardNumberMaskDirective } from './card-number-formatter.directive';

describe('CardNumberMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new CardNumberMaskDirective();
    expect(directive).toBeTruthy();
  });
});
